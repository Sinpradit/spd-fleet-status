name: update-fleet-status

on:
  schedule:
    - cron: "13,43 * * * *"   # every 30 min at off-peak minutes (avoid :00/:30 congestion)
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-fleet
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install --quiet openpyxl google-api-python-client google-auth

      - name: Run pipeline
        env:
          DTC_TOKEN: ${{ secrets.DTC_TOKEN }}
          GDRIVE_SA_KEY: ${{ secrets.GDRIVE_SA_KEY }}
        run: python pipeline/run.py

      - name: Commit data files if changed
        run: |
          git config user.name "fleet-bot"
          git config user.email "actions@github.com"
          if git status --porcelain | grep -qE "fleet-status.json|tracks.json"; then
            git add fleet-status.json tracks.json
            git commit -m "Update fleet data [skip ci] $(date -u +%FT%TZ)"
            git push
            echo "pushed update"
          else
            echo "no change"
          fi
